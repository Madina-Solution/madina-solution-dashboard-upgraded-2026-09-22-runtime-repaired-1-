# FIX LINT 2026-09-21

## Temuan dari log pengguna
- `src/app/(admin)/admin/admin-shell.tsx`: `react-hooks/set-state-in-effect` pada penutupan profile menu berdasarkan pathname.
- `src/app/(admin)/admin/admin-shell.tsx`: warning `@next/next/no-img-element` pada avatar admin.
- `src/app/(admin)/admin/finance/page.tsx`: `react-hooks/set-state-in-effect` saat loading data finance.
- `src/app/(public)/checkout/checkout-content.tsx`: warning `react-hooks/incompatible-library` dari React Hook Form `watch()`.

## Perbaikan
- Profile menu tidak lagi disinkronkan dengan `pathname` melalui effect. Menu ditutup langsung ketika link internal di dalam menu dipilih.
- Avatar admin menggunakan `SiteImage` yang sudah tersedia di project.
- Loader finance dibuat sebagai async synchronization effect dan callback `load` tidak lagi bergantung pada `form.categoryId`, sehingga perubahan kategori tidak memicu reload data.
- Checkout menggunakan `useWatch({ control, name: "customer" })` sebagai API subscription yang kompatibel dengan React Compiler.

## Verifikasi lokal dalam paket
- Semua file `.ts` dan `.tsx` pada `src/` berhasil diparse oleh TypeScript compiler API (syntax check).
- Tidak ada lagi pola target lint sebelumnya pada file yang diperbaiki.
- Log pengguna menunjukkan `npm run typecheck` sudah PASS dan `npm run build` sudah PASS; failure sebelumnya hanya terjadi pada lint.

## Catatan
`node_modules` tidak disertakan dalam ZIP. Jalankan `npm install` pada mesin pengembangan sebelum validasi release penuh.
